import styled from 'styled-components';

const MainStyle = styled('div')`
  #person-image {
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;

    margin: 0 auto;
  }
`;

export default MainStyle;
