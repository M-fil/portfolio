import styled from 'styled-components';

const AppStyle = styled('div')`
  html {
    font-size: 10px;
    line-height: 10px;
  }

  body {
    padding: 0;
    margin: 0;
  }

  #main-content {

  }
`;

export default AppStyle;
